<swiper indicator-dots="true" autoplay="true" interval="3000">
  <block wx:for="{{swiperImgUrls}}">
    <swiper-item>
      <image src="{{item.url}}" mode="widthFix" class="img">
      </image>
    </swiper-item>
  </block>
</swiper>

<view>
  <block wx:for="{{playlist}}">
  <x-playlist playlist="{{item}}">这是一个组件</x-playlist>
  </block>  
</view>
